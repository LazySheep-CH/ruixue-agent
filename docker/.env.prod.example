# 生产环境变量模板 —— 复制成 .env.prod 后填真值。
#
#   cp docker/.env.prod.example docker/.env.prod
#
# .env.prod 已在 .gitignore 里,【绝不要提交】。
# 这个 .example 文件的作用是:让人知道有哪些变量要配,而不用去翻代码。

# ── 模型服务 ──────────────────────────────────────────────────
DEEPSEEK_API_KEY=sk-填你的真实key

# 数据库凭据【不在这里配】—— 它们在 docker/.env 里,compose 会先加载 .env
# 再加载本文件。同一个密码写两份文件,迟早只改一份然后连不上。

# ── 访问控制 ──────────────────────────────────────────────────
# 程序化调用的 API Key,多个用逗号分隔,格式 key:用户名
RUIXUE_API_KEYS=
# 登录态签名密钥。必须换 —— 用默认值等于任何人都能伪造登录态。
# 生成:python -c "import secrets;print(secrets.token_urlsafe(32))"
JWT_SECRET=
# 前端域名白名单。走 nginx 同源反代时其实用不到跨域,
# 但如果前端单独部署在别的域名下,这里必须列出来。
CORS_ORIGINS=https://你的域名

# ── MCP server:把我们的工具暴露给【外部 agent】用(默认关闭)────
# 只有在确实要给第三方接入时才开。开着 = 多一个对外入口。
RUIXUE_MCP_SERVER=
# 只暴露这几个工具(逗号分隔);留空 = 全部 9 个。
# 建议按需最小化:对方要预测就只给 predict_*,不必把知识问答一起开出去
# (知识问答每次调用要花我们一次 LLM 的钱,虽然已计入 DAILY_CHAT_QUOTA)。
RUIXUE_MCP_TOOLS=
# ⚠ 开了 MCP 就【必须】填这一项:SDK 会校验 Host 头防 DNS 重绑定攻击,
#   默认只信 localhost。部署到真域名后不填,MCP 端点会全量返回 421,
#   报错只有一句 "Invalid Host header" —— 鉴权通、路由通,只在最后一步挂。
MCP_ALLOWED_HOSTS=你的域名

# ── 容量与配额(含义见 docs/运维手册.md「并发与容量」)──────────
MAX_CONCURRENT_RUNS=8      # 每 worker 同时在跑的 agent 上限
MAX_QUEUED_RUNS=16         # 排队上限,超出直接 503
DAILY_CHAT_QUOTA=200       # 单用户每日对话上限

# ── 端口 / 镜像标签【不在这里】──────────────────────────────────
# 它们是 compose 的 ${...} 插值变量,只能写在 docker/.env 里。
# env_file(也就是本文件)只负责把变量塞进【容器内部】,对插值不起作用 ——
# 写在这里会静默失效,端口永远是默认值。实测踩过。
