# 打镜像时【不】拷进去的东西 —— 减小镜像、加快构建、避免泄密。
#
# ⚠⚠ 语法陷阱:.dockerignore 只支持【整行注释】,不支持行尾注释。
#     写成  data/    # 原始数据不进镜像
#     Docker 会把整行(含空格和中文)当作一个字面模式去匹配,于是这条规则
#     **静默失效**,既不报错也没有任何提示。
#
#     实测代价:本文件原本就是这么写的,结果 data/(4.2G)、backups/(158M)
#     全被打进构建上下文 —— 光传上下文 5 分钟、5.65GB;更要命的是
#     `.env` 和 `config/config.yaml` 这两条【保护密钥】的规则也一起失效了,
#     API Key 就这么进了镜像。
#
#     所以:注释一律单独一行。改完请用下面这条命令核对上下文大小:
#         docker build --no-cache --progress=plain -f Dockerfile . 2>&1 | grep "transferring context"

# ── 虚拟环境与缓存 ──
.venv/
__pycache__/
*.pyc
.pytest_cache/
.pytest_tmp/
.ruff_cache/

# ── 版本控制与 CI ──
.git/
.github/

# ── 数据与产物(4.2G,重跑管道就有,绝不进镜像)──
#
# ⚠ 用 data/* 而不是 data/ —— 整目录被忽略时,下面的例外行不生效。
#   (和 .gitignore 里同样的写法,同样的理由。)
data/*
# 例外:data/predictors/ 只有 1.2MB,但 county_soil.csv / county_latlon.csv
# 是【运行时刚需】—— 土壤查询、按地点预测都要读它们。
# 实测踩过:整个 data/ 排掉后,容器里的工具全报 FileNotFoundError,
# 而且是【线上静默失败】——用户直接看到 "FileNotFoundError" 这种字眼。
# 比 models/ 那次更坏:那次构建期就炸了,这次一路跑到用户面前。
!data/predictors/
mineru-out/
backups/
outputs/
runs/

# ── 前端有自己的镜像(frontend/Dockerfile),别把 node_modules 塞进后端 ──
frontend/

# ── 文档与设计稿 ──
docs/
tests/
design-system/
.impeccable/
*.md

# ── 其他工具的工作目录(含 node_modules)──
.codex-work/

# ── 密钥:绝不进镜像。凭据一律运行时用环境变量注入 ──
.env
.env.*

# ⚠ config/config.yaml 【不】排除 —— 它是"配置即代码":里面写的是
#   api_key: $DEEPSEEK_API_KEY 这样的占位符,运行时才从环境变量解析,本身不含密钥。
#   排除掉会让 app 一启动就 FileNotFoundError(实测踩过)。
#   "万一有人写了明文 key"这个风险不靠约定管,靠 Dockerfile 里的构建期断言拦。

# ⚠ models/ 【不】排除 —— 预测模型产物必须随镜像走(见 Dockerfile ④)。
# ⚠ scripts/ 也不排除:训练脚本不大,而且报错信息里会让用户去跑它。
