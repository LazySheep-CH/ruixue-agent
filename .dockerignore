# 打镜像时【不】拷进去的东西 —— 减小镜像、加快构建、避免泄密。
.venv/
__pycache__/
*.pyc
.pytest_cache/
.pytest_tmp/
.git/
.github/
data/                 # 原始/中间数据,不进镜像
docs/
tests/
*.md
frontend/             # 前端有自己的镜像(frontend/Dockerfile),别把 node_modules 塞进后端
backups/              # 数据库备份,几百 MB,绝不进镜像
outputs/              # 训练/分析的中间产物
design-system/
# 注意 models/ 【不】排除 —— 预测模型产物必须随镜像走(见 Dockerfile ④)。
# scripts/ 也不排除:训练脚本不大,但报错信息里会让用户去跑它,留着方便排查。
.env                  # 密钥绝不进镜像!
config/config.yaml    # 本地配置(含 key),运行时用挂载/环境变量注入
