"""Agent 级评测。

和 RAG 检索评测(scripts/run_eval.py,量 Recall@k / MRR)的区别:
那一层只回答"有没有把对的资料捞上来";这一层回答"整个 agent 到底能不能把
用户的活干成"—— 工具选得对不对、该拒答时拒没拒、注入挡没挡住、花了多少钱。

为什么要分成两层:
    Tier A(确定性)  纯函数判分 + 假模型 → 每次 CI 跑,免费、秒级、结果固定
    Tier B(端到端)  真调大模型跑冻结评测集 → 手动/定期跑,花钱、分钟级、有波动

只有 Tier B 没有 Tier A:评测代码自己出 bug 你不会知道(判分函数写错,
所有版本一起虚高)。只有 Tier A 没有 Tier B:测的是自己的假模型,不是 agent。

主指标为什么坚持"客观可判定":
最容易走偏的做法是拿一个大模型当裁判打分(LLM-as-judge)。问题在于:
同一个版本跑两次,裁判给分能差好几个点。这时"新版本 +2%"到底是真提升还是
噪声?没法回答的指标等于没有指标。

所以主指标全部是规则可判的:工具集合精确匹配、关键点正则命中、拒答布尔、
成本计数。裁判模型只用来看"读起来顺不顺"这种实在没法规则化的,且不进主指标。

还有一件必须做的事:先量噪声地板:
大模型有温度,同一版本连跑两次结果就不一样。所以任何"新版本更好"的结论,
都必须先知道同版本重复跑的标准差有多大。差异没超过噪声地板 = 什么都没证明。
见 report.compare()。
"""
