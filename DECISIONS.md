# 技术决策记录

每条记录一个决策:**问题 → 选择 → 依据(尽量是实测数据)→ 代价**。
按这个格式,是因为半年后回看时,"当时为什么这么选"比"选了什么"更重要。

---

## 1. 解析器:MinerU,不用 GROBID

- **问题**:PDF 提取正文。GROBID 是学术圈标配。
- **选择**:MinerU。
- **依据**:同一篇中文论文,GROBID 提到 3,571 字符、标题字段为空;MinerU 提到 13,919 字符。GROBID 是英文语料训练的。
- **代价**:MinerU 更慢、更重(要 GPU)。但入库是一次性的,慢可接受。

## 2. 统一中间结构(IR)

- **问题**:以后要支持 Word、OCR 等多种解析器。每加一种都改下游?
- **选择**:定义 `Document / Element / Chunk`(Pydantic),所有解析器产出它,下游只认它。
- **依据**:解耦。加新解析器时下游零改动。
- **代价**:多一层抽象。但 Pydantic 的 `@field_validator` 顺带把脏数据挡在入口(Parse, don't validate)。

## 3. 去重:MinHash + LSH,不用两两比对

- **问题**:1,733 篇近似去重,两两算 Jaccard 是 O(n²) = 150 万次。
- **选择**:MinHash(128 维签名)+ LSH 分桶。
- **依据**:48.5 分钟 → 49.3 秒(59×),抓出 150 篇近似重复(人工抽查:精确相似度 0.959~1.000,全真)。曾试 SimHash,放弃:汉明距离阈值随文档长度漂移,定不出通用阈值。
- **代价**:概率性,有极小概率漏判。

## 4. 切块:Small-to-Big(父子块)

- **问题**:检索要短才准(长文本向量语义被平均),生成要长才全。需求相反。
- **选择**:父块=一整节,子块=父块切碎;子块进向量库负责被搜到,命中后返回父块给大模型。
- **依据**:各取所长。章节路径由栈算法从 MinerU 的扁平标题层级重建。
- **代价**:块数翻几倍(父 39,396 + 子 223,386),存储和 embedding 成本上升。
- **附带正确性**:纯公式的子块(3,454 个)虽几乎不会被自然语言命中,但公式本身在**父块**里(父块=整节含公式),命中该节任一文字子块 → 返回父块 → 公式一并带出。所以无需过滤纯公式子块 —— 内容不丢,多占的向量空间(约 7MB)可忽略。

## 5. 存储:文本进 PostgreSQL,Milvus 只存向量

- **问题**:文本存哪?教程多是直接塞向量库。
- **选择**:PostgreSQL 存全部数据,Milvus 只存 `chunk_id + 向量 + 过滤字段`。
- **依据**:**数据是资产,索引是派生物**。① 灌 26 万条中途会挂,PG 有主键+upsert 能幂等重跑,Milvus 无事务;② 数据要能改,PG 一句 UPDATE,Milvus 要删了重插整条。文本进 Milvus 会让它既是索引又是数据源,永远不敢重建索引。实测:元数据过滤 PG 6.6ms vs JSON 文件 1384ms(210×)。
- **代价**:两套存储、要维护一致性(靠"索引可重建"化解)。

## 6. 数据库变更:Alembic,不用手写 SQL

- **问题**:表结构管理。
- **选择**:SQLAlchemy 模型 + Alembic 迁移。
- **依据**:`CREATE TABLE IF NOT EXISTS` 遇到已存在的表直接跳过、加列时假装成功 → 环境静默不一致、无法回滚、无法追溯。Alembic 每次变更是一个可回滚的版本。等价于 Java 生态的 Flyway。
- **代价**:多一套命令、autogenerate 偶尔漏东西(触发器、部分 import)要手工补。

## 7. 幂等:内容寻址 + upsert

- **问题**:灌 26 万条中途失败怎么恢复?
- **选择**:`document_id = sha256[:16]`(内容寻址)+ `INSERT ... ON CONFLICT DO UPDATE`。
- **依据**:同一文件永远同一 ID → 天然幂等,挂了直接重跑、撞上的跳过。不用"先查再插"(两条语句有并发窗口,会双插冲突)。
- **代价**:带 upsert 比裸插慢 46%(84s → 122s)。26 万级其实不太值,千万级(约 53 分钟)才回本 —— 买的是"敢重跑"。

## 8. GPU:显式声明 CUDA 版 torch

- **问题**:插着 RTX 3090,`torch.cuda.is_available()` 却是 False。
- **选择**:`pyproject.toml` 指定 torch 从 PyTorch 的 cu126 源装。
- **依据**:PyPI 默认的 Windows torch 是 CPU-only 构建(版本号带 `+cpu`),sentence-transformers 顺带装了它,不报错只是慢 10-50 倍。另外 `batch_size` 从默认 32 调到 256,吞吐 +43%(874 → 1247 条/秒)。
- **代价**:torch wheel 2.5GB,依赖锁定到特定 CUDA 版本。

## 9. 中文分词:jieba,不装 zhparser 扩展

- **问题**:PG 的 `simple` 全文配置靠空格切词,中文没空格 → 整句一个 token,实测搜「地膜厚度」「PBAT」「0.010」全部失败。
- **选择**:Python 侧 jieba 分词写入 `text_tokens` 列,PG 触发器据此生成 tsvector。
- **依据**:zhparser 要自己 build 镜像、升级 PG 就得重来、分词逻辑是黑盒。jieba 在 Python 侧能加领域词典、能写单元测试、能改。领域词典让"全生物降解地膜"不被切碎,正则保护标准号「GB/T 35795」不被打散。上标统一成 plain(实测:embedding 不在乎,但 BM25 层 g·kg-1 和 g·kg⁻¹ 分词不同、互相搜不到,而用户键盘只打得出 plain)。
- **代价**:多一列 + 一次 26 万行回填 + 一份要人工维护的领域词典。

## 10. 词法检索:只用罕见词查

- **问题**:BM25 直接把查询所有词做 OR,一条长问句捞出 2.8 万候选,24.6 秒且不准。
- **选择**:按文档频率(term_df 表)挑出罕见词(DF<1%)再查。
- **依据**:BM25 靠 IDF 定位,常见词把罕见词的定位能力淹没。实测:全部词 OR 的 R@10 0.350、8497ms;只留罕见词 R@10 0.525、140ms（召回 +50%,快 60 倍）。延迟正比于候选数,候选数正比于词的常见程度 —— BM25 恰好在它有用时快、没用时慢。
- **代价**:要维护词频表(派生数据,可重建)。这条决策修正了三个 bug 才得到,过程见 `rag/bm25.py` 顶部。

## 11. 融合:RRF,不做加权分数相加

- **问题**:向量给余弦(0.4~0.9),BM25 给 ts_rank(0.0x),量纲和分布完全不同,怎么合?
- **选择**:RRF(Reciprocal Rank Fusion),只看排名不看分数。
- **依据**:直接加分数等于让尺度大的那方说了算,而尺度和"谁更准"无关。RRF 用序数,量纲无关、鲁棒、几乎不用调参。
- **代价**:丢掉了分数里的置信度信息(领先很多和领先一点,RRF 里一样)。

## 12. 融合权重:0.3,但更重要的是"这个值不重要"

- **问题**:向量:BM25 的 RRF 权重比取多少?
- **选择**:向量 1.0,BM25 0.3。
- **依据**:用交叉验证 + 自助置信区间选的,不是手调挑最高。结论:权重 0.2~0.7 的 R@3 全在 0.82~0.833(系统对权重不敏感,是平台不是尖峰);5 折交叉验证各自选出的"最优"在 0.25~0.65 乱跳(说明具体值在拟合噪声);但"混合优于纯向量"是显著的(自助 1000 次,R@3 提升 95% 置信区间 [+0.027,+0.127] 不含 0)。取 0.3:平台区内、离悬崖(>1.0 时 R@3 崩)远、偏保守。
- **代价**:换语料/换真实问题都要重跑 `scripts/tune_weight.py`,不跨数据集通用。

## 13. 重排:cross-encoder,放在检索最后

- **问题**:R@10 已达 0.900(答案在前 10),但 R@1 只有 0.647(第 1 名 35% 是错的)。
- **选择**:bge-reranker-v2-m3,对检索出的候选精排。
- **依据**:向量/BM25 把 query 和文档分开编码(快但粗);cross-encoder 把两者拼一起打分(精但慢)。检索负责召回,重排负责精排。实测 R@1 0.647 → 0.807(+16),自助置信区间显著。只能对少量候选做(30 个 97ms),所以排在最后。
- **代价**:在线延迟从 420ms 升到约 1500ms。生产上可换更小的 reranker 或砍候选池。

## 14. 生成:强制 grounding

- **问题**:直接把检索结果喂大模型,它会用自己记忆里的旧知识补充/猜测。
- **选择**:system prompt 强制"只依据提供的材料回答、标引用、数字与原文一致、没有就说没有"。
- **依据**:这是 RAG 和"直接问大模型"的本质区别 —— 答案可溯源、跟着知识库更新。地膜标准答错要赔钱。实测中系统甚至会主动报告两段材料的数值冲突而非瞎选一个。
- **代价**:答案受限于检索质量,检索没捞到就答"资料里没有"(这是对的行为,不是缺陷)。

---

## 已知技术债

- **原始 PDF** 还在本地文件系统,多机部署时须进对象存储(MinIO)。
- **评测集是合成的**,措辞偏向原文、分数偏高。上线后须用真实 query 日志逐步替换。
- **标准语料只有 37 份**,真实地膜标准体系远不止 → 语料缺口。
- **重排延迟约 1500ms**,交互式问答偏慢,须优化(小模型 / 砍候选池)。
- **未做 agent 化** — 当前是"查一次答一次",尚未让大模型自主决定何时查、查几次、改写查询。
